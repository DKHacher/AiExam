description: qwen 2.5 7b test

options:
  disableCache: true

providers:
  - id: http
    config:
      url: "http://localhost:8000/chat"
      method: POST
      headers:
        Content-Type: "application/json"
      body:
        message: "{{input}}"
        model: "qwen2.5:7b"
      transformResponse: "json.response"

prompts:
  - "{{input}}"

commandLineOptions:
  maxConcurrency: 1
  repeat: 1
  delay: 1000
  cache: false


tests: tests.yaml


